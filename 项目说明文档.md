# 项目说明文档

## 1. 项目简介

本项目是一个用于深度学习工程师技术面试的题目仓库，主要围绕“如何在已有视觉检测系统上进行能力扩展”展开。项目共包含两道题目，分别对应两个典型的工程场景：

- **题目一：目标检测类别扩展**
  - 在已有 YOLO 安全监控系统基础上，新增识别 `Green Cone`
  - 重点考察数据增强、训练思路、验证与部署优化

- **题目二：属性识别与业务告警流水线整合**
  - 在已有检测能力基础上，进一步扩展人员属性识别和业务规则告警
  - 重点考察数据结构设计、业务逻辑整合、时序告警机制与测试验证

项目目标主要不是追求复杂模型本身，而是考察候选人是否具备完整的工程化思维，包括：

- 需求分析能力
- 数据设计与标注能力
- 模型选择与训练策略设计能力
- 业务逻辑抽象能力
- 代码实现能力
- 单元测试与验证能力
- 文档表达与交付能力

---

## 2. 目录结构

```text
.
├── README.md
├── 项目说明文档.md
├── task1/
│   ├── color.md
│   ├── data_color_aug.py
│   └── test_data_color_aug.py
└── task2/
    ├── attribute.md
    ├── alert_pipeline.py
    └── test_alert_pipeline.py
```

### 目录说明

- `README.md`
  - 仓库首页说明，包含项目概览、运行方式与文件索引

- `项目说明文档.md`
  - 更详细的项目说明文档，用于描述每个任务的设计思路、实现内容和交付内容

- `task1/`
  - 题目一相关内容
  - 包含题目说明、数据增强脚本和测试脚本

- `task2/`
  - 题目二相关内容
  - 包含题目说明、告警流水线实现和测试脚本

---

## 3. 题目一说明：新增检测类别（Green Cone）

### 3.1 题目目标

题目一要求在现有 YOLO 安全监控系统的基础上，新增识别 `Green Cone`，并且尽量复用已有系统，降低重新训练和上线成本。该题重点考察的是：

- 是否能够理解“新增类别但目标外形相似”的场景
- 是否能通过数据增强和微调而不是从头训练来降低成本
- 是否能对数据、标注、验证和部署给出完整方案

### 3.2 已完成内容

题目一已经完成以下文件：

- `task1/color.md`
  - 题目分析与完整回答
  - 包含数据层、标注层、模型层、训练层、验证层和部署层的详细说明

- `task1/data_color_aug.py`
  - 数据增强脚本
  - 实现了红色锥桶到绿色锥桶的颜色迁移
  - 实现了多种几何与光照增强
  - 支持按 `2^N` 组合生成增强样本

- `task1/test_data_color_aug.py`
  - 数据增强结果验证脚本
  - 用于检查输出数量、文件命名和输出图片可读性

### 3.3 核心实现思路

#### 颜色迁移

为了低成本扩展绿色锥桶样本，脚本没有重新采集大量绿色锥桶数据，而是采用 HSV 空间进行颜色迁移：

1. 将输入图像从 BGR 转换到 HSV
2. 找到红色区域对应的 hue 范围
3. 将红色区域的 hue 平移到绿色附近
4. 再转回 BGR，得到绿色锥桶图像

这种做法的好处是：

- HSV 空间更适合做颜色迁移
- 不容易破坏图像结构
- 能保留锥桶形状和场景背景

#### 增强组合逻辑

脚本定义了多个增强类型，例如：

- 水平翻转
- 仿射变换
- 尺度缩放
- 亮度变化

每种增强都可以视为一个二值开关，即“启用”或“关闭”。因此如果共有 `N` 种增强方式，则总组合数为 `2^N`。脚本对每一张输入图片先做颜色迁移，再遍历所有增强组合，最终输出：

- `10 × 2^N` 张图片

这里的 `10` 来自题目要求，即输入目录中准备 10 张测试图片。

### 3.4 数据层设计说明

题目一的文字答案集中在 `task1/color.md` 中，内容覆盖了：

- 如何收集训练数据
- 需要多少数据量
- 如何保证数据质量
- 如何划分 Train / Validation / Test
- 为什么优先使用 Fine-tune
- 如何调整训练参数
- 如何验证新增类别不会影响已有类别
- 如何在 Jetson Orin 上部署和优化

### 3.5 运行方式

```bash
python task1/data_color_aug.py
```

运行后，脚本会将增强结果写入 `task1/output_images/`（如果目录存在或自动创建），并输出生成数量统计。

### 3.6 验证重点

对于题目一，验证关注点主要有：

- 绿色锥桶样本是否生成成功
- 输出数量是否符合 `10 × 2^N`
- 输出图片是否可正常读取
- 增强后图像是否仍保留锥桶主体特征

---

## 4. 题目二说明：属性识别扩展

### 4.1 题目目标

题目二要求在已有检测系统基础上，进一步识别人员属性，并将结果接入业务规则告警逻辑。具体业务需求包括：

- 识别人员相关属性，例如安全帽颜色、衣服颜色、是否穿反光背心
- 在业务端识别违规行为
- 实现告警触发、持续报警与 cooldown 机制

该题重点考察的是：

- 是否能设计清晰的检测结果数据结构
- 是否能将属性识别结果和业务规则结合
- 是否能处理时序告警逻辑
- 是否能通过测试脚本验证业务行为

### 4.2 已完成内容

题目二已经完成以下文件：

- `task2/attribute.md`
  - 题目原文与整理后的答案
  - 包含系统设计、数据采集、标注、模型选择、推理流程和部署方案

- `task2/alert_pipeline.py`
  - 业务端告警流水线实现
  - 定义了检测结果结构
  - 实现了安全区域 Mock 函数
  - 实现了违规判定与 cooldown 逻辑

- `task2/test_alert_pipeline.py`
  - 单元测试文件
  - 演示首次告警、持续报警与 cooldown 行为

### 4.3 核心实现思路

#### 数据结构设计

为了让业务端能够接收带属性的检测结果，代码中定义了检测输出结构，包含：

- 检测时间戳 `timestamp`
- 检测帧图片 ID `frame_id`
- 检测相机 ID `camera_id`
- 检测框列表 `detections`
- 类别名称映射 `class_name_map`

其中 `Detection` 支持：

- `track_id`：用于跨帧关联同一目标
- `parent_track_id`：用于把 helmet / vest 等从属目标关联到 person
- `attributes`：用于保存颜色、是否反光等属性信息

#### 安全区域 Mock

题目要求实现 `is_in_safety_zone()` 用于调试。当前实现支持：

- 固定返回 True / False
- 随机返回
- 按时间片段模拟人员进出区域
- 在提供多边形区域时，可以进行几何判断

这样既能满足联调，也便于后续替换成真实业务区域判定逻辑。

#### 告警规则

业务规则主要包括两类：

- 未佩戴安全帽
- 未穿指定颜色工装马甲，且位于安全区域内

告警逻辑进一步支持：

- **首次违规立即告警**
- **持续违规持续报警**
- **违规消除后进入 5 秒 cooldown**
- **cooldown 期间不重复报警**

### 4.4 测试说明

单元测试文件 `task2/test_alert_pipeline.py` 用于验证：

1. 首次违规时是否触发告警
2. 违规持续时是否持续报警
3. 违规消除后是否停止告警
4. cooldown 期间是否抑制重复报警
5. cooldown 结束后是否恢复报警

测试命令为：

```bash
pytest task2/test_alert_pipeline.py -q
```

### 4.5 业务层设计说明

题目二的文字答案集中在 `task2/attribute.md` 中，内容覆盖了：

- 数据采集方案
- 是否需要重新标注
- 标注方案与颜色定义
- 如何保证多人标注一致性
- 模型结构如何选择
- 推理流程如何组织
- 如何降低额外计算开销
- 如何保证部署实时性

---

## 5. 交付文件说明

以下文件属于主要交付内容：

- `README.md`
- `项目说明文档.md`
- `task1/color.md`
- `task1/data_color_aug.py`
- `task1/test_data_color_aug.py`
- `task2/attribute.md`
- `task2/alert_pipeline.py`
- `task2/test_alert_pipeline.py`

### 文件用途概览

- **说明类文件**
  - `README.md`
  - `项目说明文档.md`
  - `task1/color.md`
  - `task2/attribute.md`

- **代码实现类文件**
  - `task1/data_color_aug.py`
  - `task2/alert_pipeline.py`

- **测试类文件**
  - `task1/test_data_color_aug.py`
  - `task2/test_alert_pipeline.py`

---

## 6. 使用说明

### 6.1 题目一运行

```bash
python task1/data_color_aug.py
```

### 6.2 题目二测试

```bash
pytest task2/test_alert_pipeline.py -q
```

### 6.3 文档查看

建议按以下顺序查看：

1. `README.md`
2. `项目说明文档.md`
3. `task1/color.md`
4. `task2/attribute.md`

这样可以先看到项目总览，再看每个题目的详细设计与实现。

---

## 7. 备注

本仓库的实现以“题目要求 + 可交付说明 + 可运行代码 + 可验证测试”为主，强调完整性和工程性。所有代码、文档和测试之间都保持了相互对应关系，便于检查、演示和后续扩展。
